# FiveM resources

Server-side resources for the Loyalty B4 Love RP custom framework. One folder per resource, named `lbl_<name>`. See the root `CLAUDE.md` for conventions, and use the `fivem-resource` skill to scaffold new ones.
